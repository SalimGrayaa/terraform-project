name: AWS K8s Lab - Deploy Application

on:
  workflow_call:
    inputs:
      bucket_name:
        description: 'S3 Bucket Name'
        required: true
        type: string
      region:
        description: 'AWS Region'
        required: false
        default: 'us-east-1'
        type: string

env:
  AWS_REGION: ${{ inputs.region || 'us-east-1' }}

concurrency:
  group: terraform-deploy-${{ inputs.bucket_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy_application:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ./application
    env:
      TF_VAR_region: ${{ inputs.region || 'us-east-1' }}
      TF_VAR_bucket_name: ${{ inputs.bucket_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.1

      - name: Run Common Project Initialization
        uses: ./.github/actions/x-common-steps
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ inputs.region || 'us-east-1' }}
          terraform-version: latest

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: |
          terraform init -backend=false
          terraform validate
          terraform init \
            -backend-config="bucket=${{ inputs.bucket_name }}" \
            -backend-config="key=terraform.tfstate" \
            -backend-config="region=${{ inputs.region || 'us-east-1' }}"

      - name: Terraform Plan
        run: terraform plan -out=tfplan -var="region=${{ inputs.region || 'us-east-1' }}" -var="bucket_name=${{ inputs.bucket_name }}"

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
      
      # - name: install k8s tools ( kubelet, kubeadm and kubectl) in the Public EC2 instance
      #   env:
      #     DEBIAN_FRONTEND: noninteractive
      #   run: |
      #     instance_public_ip=$(terraform output -raw instance_public_ip)
      #     key_path=$(terraform output -raw key_path)

      #     echo "Waiting for SSH on ${instance_public_ip} (timeout 5m)"
      #     timeout=300
      #     interval=10
      #     elapsed=0
      #     until ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=5 -i "$key_path" ubuntu@${instance_public_ip} 'echo ssh-ok' &>/dev/null; do
      #       if [ "$elapsed" -ge "$timeout" ]; then
      #         echo "Timed out waiting for SSH on ${instance_public_ip}" >&2
      #         exit 1
      #       fi
      #       sleep $interval
      #       elapsed=$((elapsed + interval))
      #       echo "Retrying SSH... elapsed ${elapsed}s"
      #     done

      #     scp -o StrictHostKeyChecking=no -i "$key_path" scripts/install_kubectl.sh ubuntu@${instance_public_ip}:/home/ubuntu/install_kubectl.sh
      #     ssh -o StrictHostKeyChecking=no -i "$key_path" ubuntu@${instance_public_ip} "/bin/bash /home/ubuntu/install_kubectl.sh && rm -f /home/ubuntu/install_kubectl.sh"
      # - name: Create AMI Image from the Public EC2 instance
      #   run: |
      #     instance_id=$(terraform output -raw instance_id)
      #     image_name="k8s-ami-$(date +%Y%m%d%H%M%S)"
      #     aws ec2 create-image --instance-id "$instance_id" --name "$image_name" --no-reboot

      - name: Destroy Terraform Resources on Failure
        if: ${{ failure() }}
        run: terraform destroy -auto-approve -var="region=${{ inputs.region || 'us-east-1' }}" -var="bucket_name=${{ inputs.bucket_name }}"